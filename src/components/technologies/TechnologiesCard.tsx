const Technologies = ({ technologies }: any) => {

    return (
        <div>
            {
                technologies.map((technology)=>{

                })
            }
        </div>
    );
};

export default Technologies;